/node_modules
/package-lock.json
# Environment varibales
config.DEVELOPMENT.env

# macOS
.DS_Store

# Production env
config.PRODUCTION.env

# Logs
*.log
/logs

# Locally-stored uploads (dev only; production uses Cloudinary/S3)
/uploads

# Google OAuth client secret JSON (never commit)
client_secret*.json
